pipeline{
    agent any
    stages{
        stage('Build'){
            steps{
                echo 'Building Okay..'
                sh "whoami"
                sh "echo $PATH"
                sh 'mvn clean package -X'
                sh "docker build . -t demo:${env.BUILD_ID}"
            }
        }
    }
}